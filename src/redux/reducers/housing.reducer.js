import {
    SECONDARY,
    BRIDALCHAMBER,
    RENT
} from '../creator_name'

const initState = {

}

export default (state = initState, action) => {
    switch (action.type) {
        case SECONDARY:

            break;
        case BRIDALCHAMBER:

            break;
        case RENT:

            break;

        default:
            return state;
    }
}