# For more information on configuration, see:
#   * Official English Documentation: http://nginx.org/en/docs/
#   * Official Russian Documentation: http://nginx.org/ru/docs/

user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log;
pid /run/nginx.pid;

# Load dynamic modules. See /usr/share/nginx/README.dynamic.
include /usr/share/nginx/modules/*.conf;

events {
    worker_connections 1024;
}

http {
    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile            on;
    tcp_nopush          on;
    tcp_nodelay         on;
    keepalive_timeout   65;
    types_hash_max_size 2048;

    include             /etc/nginx/mime.types;
    default_type        application/octet-stream;

    # Load modular configuration files from the /etc/nginx/conf.d directory.
    # See http://nginx.org/en/docs/ngx_core_module.html#include
    # for more information.
    include /etc/nginx/conf.d/*.conf;
    # 反向代理 market
    server {
      listen 80;
      server_name market.conjuring.cn;
      location / {
        proxy_pass http://market.conjuring.cn:3000;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $http_host;
        proxy_set_header X-Nginx-Proxy true;
        proxy_redirect off;
      }
    }
    # 反向代理 api.housing
    server {
      listen 80;
      server_name api.housing.conjuring.cn;
      location / {
        proxy_pass http://api.housing.conjuring.cn:9093;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $http_host;
        proxy_set_header X-Nginx-Proxy true;
        proxy_redirect off;
      }
    }
    # 反向代理 housing
    server {
      listen 80;
      server_name housing.conjuring.cn;
      location / {
        proxy_pass http://housing.conjuring.cn:9090;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $http_host;
        proxy_set_header X-Nginx-Proxy true;
        proxy_redirect off;
      }
    }

    server {
        listen       80;
        server_name  _;
        root         /usr/share/nginx/html;

        # Load configuration files for the default server block.
        include /etc/nginx/default.d/*.conf;

        location / {
        }

        error_page 404 /404.html;
            location = /40x.html {
        }

        error_page 500 502 503 504 /50x.html;
            location = /50x.html {
        }
    }

    # 前端部署
    # server {
    #     listen 9090;
    #     server_name housing.conjuring.cn;
    #     root /home/housing/client/build;
    #     index index.html index.htm;

    #     location / {
    #         try_files $uri $uri/ /index.html;
    #     }
    #     location ^~ /static/media/ {
    #         # 开启gzip
    #         gzip on;
    #         # 启用gzip压缩的最小文件，小于设置值的文件将不会压缩
    #         gzip_min_length 1k;
    #         # 缓存时间
    #         expires max;
    #         add_header Cache-Control public;
    #     }

    #     error_page 500 502 503 504 /50x.html;
    #     client_max_body_size 20M;
    #     keepalive_timeout 10;
    # }

# Settings for a TLS enabled server.
    # https反向代理 market
    server {
        listen       443 ssl;
#        listen       [::]:443 ssl http2 default_server;
        server_name  market.conjuring.cn;
        root         /usr/share/nginx/html;
        ssl on;
        ssl_certificate "cert/a.pem";
        ssl_certificate_key "cert/a.key";
#        ssl_session_cache shared:SSL:1m;
        ssl_session_timeout  5m;
        ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
        ssl_ciphers ECDH:AESGCM:HIGH:!RC4:!DH:!MD5:!aNULL:!eNULL;
        ssl_prefer_server_ciphers on;

#        # Load configuration files for the default server block.
        include /etc/nginx/default.d/*.conf;
        location / {
            proxy_pass http://market.conjuring.cn:3000;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header Host $http_host;
            proxy_set_header X-Nginx-Proxy true;
            proxy_redirect off;
        }

        error_page 404 /404.html;
            location = /40x.html {
        }

        error_page 500 502 503 504 /50x.html;
            location = /50x.html {
        }
    }
    # https反向代理 api.housing
    server {
        listen       443 ssl;
#        listen       [::]:443 ssl http2 default_server;
        server_name  api.housing.conjuring.cn;
        root         /usr/share/nginx/html;
        ssl on;
        ssl_certificate "cert/a.pem";
        ssl_certificate_key "cert/a.key";
#        ssl_session_cache shared:SSL:1m;
        ssl_session_timeout  5m;
        ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
        ssl_ciphers ECDH:AESGCM:HIGH:!RC4:!DH:!MD5:!aNULL:!eNULL;
        ssl_prefer_server_ciphers on;

#        # Load configuration files for the default server block.
        include /etc/nginx/default.d/*.conf;
        location / {
            proxy_pass http://api.housing.conjuring.cn:9093;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header Host $http_host;
            proxy_set_header X-Nginx-Proxy true;
            proxy_redirect off;
        }

        error_page 404 /404.html;
            location = /40x.html {
        }

        error_page 500 502 503 504 /50x.html;
            location = /50x.html {
        }
    }
    # https反向代理 housing
    server {
        listen       443 ssl;
#        listen       [::]:443 ssl http2 default_server;
        server_name  housing.conjuring.cn;
        root         /usr/share/nginx/html;
        ssl on;
        ssl_certificate "cert/a.pem";
        ssl_certificate_key "cert/a.key";
#        ssl_session_cache shared:SSL:1m;
        ssl_session_timeout  5m;
        ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
        ssl_ciphers ECDH:AESGCM:HIGH:!RC4:!DH:!MD5:!aNULL:!eNULL;
        ssl_prefer_server_ciphers on;

#        # Load configuration files for the default server block.
        include /etc/nginx/default.d/*.conf;
        location / {
            proxy_pass http://housing.conjuring.cn:9090;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header Host $http_host;
            proxy_set_header X-Nginx-Proxy true;
            proxy_redirect off;
        }

        error_page 404 /404.html;
            location = /40x.html {
        }

        error_page 500 502 503 504 /50x.html;
            location = /50x.html {
        }
    }

}

